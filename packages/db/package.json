{
  "name": "@ai-analyzer/db",
  "version": "1.0.0",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "generate": "prisma generate",
    "migrate": "dotenv -e ../../.env -- prisma migrate dev",
    "studio": "prisma studio",
    "push": "prisma db push"
  },
  "dependencies": {
    "@prisma/client": "^5.8.0",
    "dotenv": "^16.4.1"
  },
  "devDependencies": {
    "dotenv-cli": "^7.3.0",
    "prisma": "^5.8.0",
    "typescript": "^5.3.3"
  }
}
